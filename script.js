let printNumbersTill = (n) => {
    for(let counter = 1; counter <= n; counter++){
        console.log(counter);
    }
}
let getGreetingTo = (name) => {
    return "Hello " + name;
}
let myLista = [0,3,6,7,9];
myLista.forEach((number) => {
    console.log(number);
});